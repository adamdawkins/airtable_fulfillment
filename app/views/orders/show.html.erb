<div class="container mx-auto my-8 px-4">
  <div class="max-w-3xl mx-auto">
    <div class="flex justify-between items-center mb-4">
      <h1 class="h3"><%= link_to 'Orders', orders_path %> > Order #<%= @order.id %></h1>
      <%= link_to 'Edit', edit_order_path(@order), class: "btn btn-link" %>
    </div>

    <div class="bg-white rounded shadow p-8">
            <div class="mb-4">
        <h5>Name</h5>
        <%= @order.name %>
      </div>
            <div class="mb-4">
        <h5>Status</h5>
        <%= @order.status %>
      </div>
            <div class="mb-4">
        <h5>Customer name</h5>
        <%= @order.customer_name %>
      </div>
            <div class="mb-4">
        <h5>Airtable</h5>
        <%= @order.airtable_id %>
      </div>
          </div>
  </div>
</div>
